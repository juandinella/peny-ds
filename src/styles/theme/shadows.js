import tokens from '../../../tokens/json/shadow.json'

const shadows = {
  sm: tokens.shadow.sm.value,
  md: tokens.shadow.md.value,
  lg: tokens.shadow.lg.value,
  xl: tokens.shadow.xl.value,
}

export default shadows
